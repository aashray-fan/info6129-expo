import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: {
    margin: 8,
    padding: 10,
    borderWidth: 1,
    borderRadius: 8,
    borderColor: "#CCCCCC",
    backgroundColor: "#fff",
  },
  descText: {
    fontSize: 18,
    fontWeight: "bold",
  },
  smallText: {
    fontSize: 14,
    marginVertical: 2,
  },
});
